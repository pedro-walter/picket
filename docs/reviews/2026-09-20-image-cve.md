# Image-CVE review — 2026-09-20

Scope: all three agents (`monitoria-soul-spike`, `soul-spike`, `registry`).
`picketctl findings --status open` returns **4 findings total, 0 of kind
`image-cve` and 0 of kind `image-tag`** — the backlog this skill exists to
triage is empty. The 4 open findings are 1 `host-health` (disk usage on
`monitoria-soul-spike`, 96.8%) and 3 `apt` (`regular-pending` on all three
agents), both out of this skill's scope.

## Why it's empty

The prior two reviews already cleared everything:

- [`2026-09-13-image-cve.md`](./2026-09-13-image-cve.md) built the
  `docker.souspike.com.br/healthchecks/healthchecks:4.4-1` overlay
  (category B).
- [`2026-09-16-image-cve.md`](./2026-09-16-image-cve.md) rescanned that
  overlay under its new subject, found 62 fresh findings (none with a
  usable fix — 60 genuinely unfixed, 2 fixed only in an unreachable
  vendored layer), and suppressed all of them at the user's explicit
  request. `rules list` confirms all 62 are present with 90-day expiries
  (`2026-12-15`), and none of them are back as open findings today.
- `registry`'s zot grpc/xDS CVE (CVE-2026-84445, commit `77374ea`) is
  likewise suppressed through `2026-12-12` and not back.

No image scan this pass surfaced anything new against any subject.

## Housekeeping observed, not actioned

Two things worth a human look, neither part of this review's job (rules
stay read-only unless separately asked):

**Stale rules for a dead subject.** 47 suppression rules still target
`--subject healthchecks/healthchecks` (the upstream repo name, `identifier
_glob`s like `*|bsdutils`, `*|perl-base`, etc. — some with `expires_at:
null`, i.e. permanent). Since `monitoria-soul-spike`'s compose file moved
to the `docker.souspike.com.br/healthchecks/healthchecks` overlay tag back
on 2026-09-13, nothing scans under the old subject string anymore, so
these 47 rules can never match a finding again. They're inert, not
harmful, but they clutter `rules list` and the several `expires_at: null`
ones among them will sit forever since nothing will ever prompt their
removal. Worth pruning in a separate pass — not done here since it's a
rules-cleanup task, not an open-finding triage.

**Suppressions expiring in the next two weeks (2026-10-04):** `postgres`
(`libuuid`, and `libssl3`/`libcrypto3` under CVE-2026-14456),
`tecnativa/docker-socket-proxy` (`libssl3`/`libcrypto3`, same CVE), and
`traefik` (`libssl3`/`libcrypto3`, same CVE) — 7 rules total. When these
lapse, their findings will reopen on the next `image-scan` cycle unless
the underlying images have since shipped the fix. Worth a follow-up
review right around 2026-10-04 to re-check whether a newer tag has closed
CVE-2026-14456 upstream before just re-suppressing.

## Uncommitted state from the last review

`docs/reviews/2026-09-16-image-cve.md` and the `custom-docker/README.md`
edit that documents the overlay's build/push/compose-repoint are both
sitting uncommitted in the working tree (untracked / unstaged,
respectively) — not something this run touched or should commit on its
own, but flagging it since it's real finished work from three days ago
that isn't in git history yet.

## Summary

| action | findings | notes |
|---|---|---|
| bump tag | 0 | nothing open |
| custom rebuild | 0 | nothing open |
| suppress (90d) | 0 | nothing open — everything already suppressed by prior reviews |

0 open `image-cve` or `image-tag` findings across all 3 agents. Nothing to
classify, no new `picketctl rules add` commands to hand back this pass.
